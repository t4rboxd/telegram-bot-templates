this is just some example telegram bots in random coding languages.

enjoy it, for whatever you use it for.

usage -
python:
$ pip install python-telegram-bot --upgrade
$ python main.py

nodejs:
$ npm install node-telegram-bot-api
$ node index.js

ruby:
$ gem install telegram-bot-ruby
$ ruby bot.rb

php:
$ php bot.php